[project]
name = "audio-to-text"
version = "0.1.0"
description = "Audio to text converter using Vosk"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=13.9.4",
    "vosk>=0.3.45",
]
